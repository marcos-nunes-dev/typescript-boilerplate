import { render, screen } from '@testing-library/react';
import {{pascalCase name}} from '../index';

describe('{{pascalCase name}}', () => {
  it('should render', () => {
    render(<{{pascalCase name}} />);

    expect(screen.getByTestId({{pascalCase name}})).toBeInTheDocument();
  })
})
